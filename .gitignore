/node_modules
/.next/
/out/
/build
*.log

# Secrets. `.env.example` is the only env file that belongs in the repo.
.env
.env.*
!.env.example

.DS_Store
next-env.d.ts

# Incremental TypeScript build cache — regenerated by every tsc run.
tsconfig.tsbuildinfo

# Cached source photography for the portfolio mockup generator
.cache/
